#include<stdio.h>
#include<stdlib.h>


int main(void)
{	
	/*char letter;
	printf("Enter a letter:");
	scanf_s("%c", &letter);
	
	if (letter >= 65 && letter <= 90)
	{
		letter = letter + 32;
		printf("Transfered to %c.\n", letter);
	}
	else if (letter >= 97 && letter <= 122)
	{
		letter = letter - 32;
		printf("Transfered to %c.\n", letter);
	}
	else
	{
		printf("wrong!!\n");
	}*/

	system("pause");
	return 0;
}